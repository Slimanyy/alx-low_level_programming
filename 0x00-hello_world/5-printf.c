#include <stdio.h>
/**
 * main - entry point
 * prntf string
 * Return: return 0 after program has been executed
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
